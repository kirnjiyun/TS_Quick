function 함수(a) {
    if (a && typeof a === "string") {
    }
}
function 함수(animal) {
    // animal.swim 안된다.
    if ("swim" in animal) {
        animal.swim;
    }
}
function printAll(strs) {
    if (strs && typeof strs === "string") {
        console.log(st);
    }
}
function 함수(x) {
    if (x.wheel === "4개") {
        console.log("x는 Car타입이에요");
    }
}
